<% flash.each do |name, msg| %>
  <%= content_tag :div, :id => "flash_#{name}", :class => "alert-box", :'data-alert' => true do%>
    <span><%=msg %></span>
    <a href="#" class="close">&times;</a>
  <% end %>
<%end%>
